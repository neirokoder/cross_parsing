# todo_fix.md — сценарий доработки алгоритма парсера

Цель: устранить расхождения «результат vs эталон», выявленные автоматической сверкой
(`cli.py compare --pdf data/pdf/2-020101-174-12.pdf --etalon control_sample/2-020101-174-12.json`).

## 1. Исходные данные сверки

- Эталон: 266 блоков (ручная сборка, `control_sample/2-020101-174-12.json`).
- Результат: 371 блок (`data/output/2-020101-174-12.json`).
- Совпало: 248. **Precision 0.668 | Recall 0.932 | F1 0.779 | text_coverage 0.872**, type_mismatch 5.
- Отчёты: `data/output/report_2-020101-174-12.{json,md}` (missed/extra по типам и страницам).

Пайплайн (где живут баги):
`html_to_document_json` (html_to_json.py, структура из Docling) →
`enrich_blocks_from_pdf` (pdf_extract.py, добор текста/формул/картинок) →
`extract_formula_images` (pdf_extract.py) → `restore_bboxes` (pdf_extract.py) →
качество/сохранение (cross_parser.py).

## 2. Каталог дефектов (по отчёту)

| # | Дефект | Проявления (страницы) | Чем вызвано | Влияние |
|---|--------|----------------------|-------------|---------|
| A | Колонтитулы «Правила классификации и постройки морских судов (часть XII)» попадают в результат | ~37 страниц (2–40), + «…(часть XII)» без скобок на 3 | Docling-`<p>` пропускается без фильтра; enrich фильтрует только добавляемые им строки | ~37 extra |
| B | Дубли одинаковых блоков (2 копии) | 2 (версии ×2, одна с выпавшими словами), 3 (таблица ×2), 4 (1.1.3), 5 (определения с разрядкой «Ж и л ы е» + обычные), 6 («Объем технической документации…»), 15, 41 (адрес ×2) | Docling дублирует контент (разрядка/обычный текст), парсер не дедуплицирует | ~10 extra |
| C | Фрагменты строк, дублирующие содержимое таблиц/списков, как параграфы | 6, 8 («Т а б л и ц а 2.2.1 Группа холодильного агента…»), 13, 14, 16, 18, 19, 20, 23, 26, 28, 29, 30, 32, 33, 39 (строки табл. 12.2.2 + сноска) | Docling дублирует табличный/списочный текст в `<p>` | ~20 extra |
| D | Склейка разных пунктов в один параграф | 31 (8.1.1+8.1.2+8.1.3), 34 (10.1.4+10.1.5) | Docling склеивает в один `<p>` | 7 missed (эталонные пункты не матчатся) |
| E | Разрыв абзаца на хвостовые фрагменты | 31 («8.1.4 и 8.1.5.»), 34 («10.1.13 жилые и служебные помещения.») | Docling рвёт абзац по строкам | extra-фрагменты |
| F | Слияние 1.3.2+1.3.3+1.3.4 в один список (18 items) | 6 | Docling — один `<ul>`; парсер не режет по номерам пунктов | 2 missed |
| G | Формулы | G1: 37 — «11.3.1 … ± 5 %…» продублирована как formula + paragraph (SymbolMT попадает в math-индикаторы, text block целиком становится формулой); G2: 23 — «G= qS/r, (5.1.2)» слита со строками «где q —…», «S — площадь…»+«r — удельная…» в 2 formula-блока (math-шрифт в одной строке блока помечает весь блок); G3: 23 — пустая formula [0,0,0,0] (подмена "Formula not decoded" → `<figure class="formula"></figure>` без замены текста, т.к. formula_bboxes пуст); G4: 1 — formula-дубль логотипа (image bbox == formula image bbox) | enrich + html_to_document_json | 1 missed + 4 extra + type_mismatch |
| H | Потеряна подпись «Таблица 2.2.1» на 8 (на 39 «Таблица 12.2.2» есть) | 8 | Docling не вывел подпись; enrich отбрасывает разряженную строку «Т а б л и ц а…» (is_garbage_line/длина) | 1 missed |
| I | Сносочные маркеры приклеены к тексту | 3 («ПЕРЕЧЕНЬ ИЗМЕНЕНИЙ 1»), 39 («Сальник 1 вала компрессора») | Надстрочная цифра 6–7pt включена в текст | шум в heading/ячейках |

## 3. Сценарий исправлений

### Шаг 1. html_to_json.py — постобработка блоков (после сбора `all_blocks` в `html_to_document_json`)

**1.1 Дедупликация (B, J).**
- `norm_dedup(text)`: снять разрядку — убрать пробелы между одиночными буквами
  (`re.sub(r'(?<=\w) (?=\w)', '', s)`, «Ж и л ы е» → «Жилые»), сжать пробелы, lowercase.
- Удалять блок, если его norm-текст идентичен norm-тексту уже добавленного блока той же страницы.
- Удалять paragraph, если его norm-текст — подстрока norm-текста другого блока той же страницы
  (list/table/paragraph) и длина фрагмента < 60% длины контейнера (осторожно с короткими: порог ≥ 40 симв.).
- Проверка: p2 (версии), p3 (таблица), p5 (определения), p15, p41.

**1.2 Разбивка списков по номерам пунктов (F).**
- В list-блоке идти по items; item, начинающийся с полного номера `^\d+\.\d+\.\d+` после
  подпунктов `^\.\d+` , начинает новый блок; группа [номер, .1…, .N] — свой list (или paragraph для «1.3.3 …»).
- Проверка: p6 → [1.3.2, .1–.7], [1.3.3], [1.3.4, .1–.8].

**1.3 Сегментация параграфов по пунктам (D).**
- В paragraph разбивать по маркеру пункта в начале предложения:
  `re.split(r'(?<=\.)\s+(?=\d{1,2}\.\d+(?:\.\d+)*\s+[А-ЯA-Z«0-9])', text)`.
- Защита: маркер только после «. » с заглавной буквой/цифрой; «1.3.2.1, 1.3.2.2» внутри перечислений (после запятых) не резать; «(см. 2.2.1).» не резать.
- Проверка: p31 (8.1.1–8.1.5), p34 (10.1.4–10.1.13).

**1.4 Сносочные маркеры в heading (I).**
- У heading/paragraph срезать хвостовой маркер `\s+\d{1,2}\s*$`, если текст длиннее 20 симв. и цифра не является номером раздела (для заголовков вида «ПЕРЕЧЕНЬ ИЗМЕНЕНИЙ 1»; не трогать «1 ОБЩИЕ ПОЛОЖЕНИЯ» — там цифра в начале).
- Проверка: p3.

### Шаг 2. pdf_extract.py — `enrich_blocks_from_pdf` (G, H)

**2.1 Формулы из текстовых блоков (G1, G2).**
- Обрабатывать не весь text block, а построчно: к формуле относить только подряд идущие линии,
  где доля math-символов/шрифтов ≥ 0.3 (CambriaMath, SymbolMT и пр.); строки «где q —…»,
  «S — площадь…», «r — удельная…» (обычный шрифт) — обычный текст.
- Не создавать formula, если строка начинается с маркера пункта `^\d+\.\d+\.\d+\s` (пункты вида 11.3.1 — текст, не формула).
- Не создавать formula, если norm-текст на ≥85% содержится в существующем блоке той же страницы
  (случай p37: «11.3.1 … ± 5 %…» уже есть как paragraph).
- Проверка: p37 — только paragraph; p23 — formula «G= qS/r, (5.1.2)» отдельно, «где…» — в лист.

**2.2 Разряженные подписи таблиц (H).**
- В `soft_clean_line`/`is_garbage_line` снять разрядку («Т а б л и ц а 2.2.1» → «Таблица 2.2.1»),
  чтобы строка не отбрасывалась и добавлялась как paragraph (на p39 подпись уже есть в Docling — не дублировать, если norm-текст есть в Docling).
- Проверка: p8 — появился paragraph «Таблица 2.2.1».

**2.3 Надстрочные сносочные маркеры в тексте (I).**
- При сборке `line_texts` отбрасывать spans размером < 8pt, состоящие из цифр/одиночных символов,
  если в строке есть основной текст ≥ 8pt («Сальник 1 вала» → «Сальник вала»).
- Проверка: p39, ячейка 2 табл. 12.2.2.

### Шаг 3. pdf_extract.py — `extract_formula_images` (G4)

**3.1 Пропуск перекрытых кандидатов.**
- Перед добавлением formula-блока проверять пересечение bbox (IoU > 0.8) с существующими
  image-блоками той же страницы — пропускать (p1: formula_p1_1.png == логотип).
- Проверка: p1 — только image-блок логотипа.

### Шаг 4. cross_parser.py — пост-чистка после `restore_bboxes` (A, C, E, G3)

**4.1 Удаление колонтитулов (A).**
- После восстановления bbox: удалять paragraphs, чей текст встречается на ≥ 3 страницах
  (по norm-тексту) и позиция в верхней/нижней зоне (y1 < 0.12·h или y0 > 0.85·h; высоты из metadata.pages).
- Учесть вариант без «(часть XII)» (p3): сравнивать по нормированному тексту без скобок.
- Проверка: ни одного блока «Правила классификации и постройки морских судов…» в результате.

**4.2 Удаление перекрывающихся фрагментов (C, E).**
- Удалять блок, если его norm-текст — подстрока norm-текста другого блока той же страницы
  (таблица/список/параграф) и длина < 70% контейнера (порог длины ≥ 40 симв.).
- Покрывает: фрагменты строк табл. 2.2.1 и 12.2.2 (p8, p39), фрагменты списков (p6, p13, p29),
  хвосты «8.1.4 и 8.1.5.» (p31), «10.1.13 жилые и служебные помещения.» (p34).

**4.3 Удаление пустых формул (G3).**
- Удалять formula-блоки с пустым content и без image_key/latex (заглушки "Formula not decoded").
- Проверка: p23 — нет пустой формулы.

**4.4 (опционально) Склейка разорванных абзацев (E).**
- Если подряд идущие paragraphs одной страницы в сумме образуют текст, присутствующий в PDF
  как единый блок (сверка с `_collect_doc_text_by_page`/PDF-строками), — склеить в один.
- Применять только если 4.2 не покрывает хвосты «8.1.4 и 8.1.5.», «10.1.13…».

## 4. Критерии приёмки

Прогон (эталон не менять!):
```
python -X utf8 work\etalon_build\build_etalon.py   (только если правится эталон)
python -X utf8 cli.py compare --pdf data/pdf/2-020101-174-12.pdf --etalon control_sample/2-020101-174-12.json
```
Целевые значения:
- Precision ≥ 0.90 (сейчас 0.668; убирают ~100 extra: колонтитулы ~37, дубли ~10, фрагменты ~25, формулы-дубли 4, хвосты E).
- Recall ≥ 0.96 (сейчас 0.932; возвращают p6 (2), p8 (1), p31 (5), p34 (9), p23 формула (1)).
- F1 ≥ 0.93, text_coverage ≥ 0.95, type_mismatch = 0.
- Контроль регрессии: списки 7.2.3–7.2.7 (p29–30) не разрезаны; «где…»-строки формулы 5.1.2 (p23) в листе; подпись «Таблица 12.2.2» (p39) осталась; заголовки «1 ОБЩИЕ ПОЛОЖЕНИЯ» и т.п. не пострадали от 1.4.

## 5. Порядок и приоритет

- P1: 4.1 (колонтитулы), 1.1 (дедуп), 4.2 (фрагменты) → precision.
- P1: 1.2 (списки p6), 1.3 (сегментация p31/p34) → recall.
- P2: 2.1–2.3, 3.1 (формулы), 2.2 (подпись p8), 1.4/2.3 (сноски).
- P3: 4.4 (склейка абзацев) — только при необходимости.

Риски:
- 1.3/1.2 могут порезать корректные списки/абзацы → проверять регрессию (п.4).
- 4.2 с короткими блоками может удалить нужное → порог длины 40 симв. и проверка p39 (сноска таблицы остаётся как paragraph).
- Дефекты Docling (B, C, D) лечатся на нашей стороне пост-обработкой; менять HTML не требуется.
